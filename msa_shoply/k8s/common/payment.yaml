apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment
  namespace: shoply
  labels:
    app: payment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment
  template:
    metadata:
      labels:
        app: payment
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "4004"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: payment
          image: ghcr.io/incheon-soda/shoply-payment:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4004
          envFrom:
            - configMapRef:
                name: shoply-config
            - secretRef:
                name: shoply-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 4004
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 4004
            initialDelaySeconds: 15
            periodSeconds: 10
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: payment-svc
  namespace: shoply
  labels:
    app: payment
spec:
  selector:
    app: payment
  ports:
    - port: 4004
      targetPort: 4004
      protocol: TCP
  type: ClusterIP
