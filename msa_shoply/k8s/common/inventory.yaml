apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory
  namespace: shoply
  labels:
    app: inventory
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory
  template:
    metadata:
      labels:
        app: inventory
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "4002"
        prometheus.io/path: "/metrics"
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            preference:
              matchExpressions:
              - key: experiment-role
                operator: In
                values: ["worker1"]
      containers:
        - name: inventory
          image: ghcr.io/incheon-soda/shoply-inventory:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4002
          envFrom:
            - configMapRef:
                name: shoply-config
            - secretRef:
                name: shoply-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /livez
              port: 4002
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 15
            periodSeconds: 10
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-svc
  namespace: shoply
  labels:
    app: inventory
spec:
  selector:
    app: inventory
  ports:
    - port: 4002
      targetPort: 4002
      protocol: TCP
  type: ClusterIP
