# 작업내용

생성일: 2026년 5월 26일 오전 12:36

- 목차

---

# ▶︎ GitHub

## ▷ 브랜치 전략

main

- 최종 안정 버전

develop

- 통합 테스트 브랜치

aws

- AWS 환경 작업

on-pre

- 온프레미스 환경 작업

cicd

- CI/CD 작업

## ▷ 작업 흐름

1. develop 최신화

git pull origin develop

1. 본인 브랜치에서 작업
2. commit 및 push
3. develop으로 PR 생성
4. 통합 테스트 후 main 반영

## ▷ README 및 규칙 작성

### ▷ 공통 README

<aside>

**ON_P_VS_AWS**

온프레미스 vs AWS 기반 하이브리드 클라우드 확장 플랫폼

**프로젝트 소개**

온프레미스 환경에서 운영되는 서비스를 Kubernetes 기반으로 구성하고, 트래픽 증가 상황에서 AWS EKS 환경과 비교하여 확장성, 운영성, 모니터링 차이를 분석하는 프로젝트입니다.

온프레미스 환경은 kubeadm 기반 Kubernetes 클러스터로 구성하고, AWS 환경은 EKS 기반 Kubernetes 클러스터로 구성합니다. 동일한 애플리케이션을 양쪽 환경에 배포하여 부하 테스트와 모니터링 결과를 비교합니다.

**프로젝트 목표**

- 온프레미스 Kubernetes 환경 구축
- AWS EKS 환경 구축
- 동일 애플리케이션의 양쪽 환경 배포
- 모니터링 및 부하 테스트 기반 비교
- CI/CD 자동화 구조 설계

**프로젝트 구조**

`ON_P_VS_AWS/
├─ app/          # 공통 애플리케이션 코드
├─ database/     # 공통 DB 스키마 및 초기 데이터
├─ load-test/    # 부하 테스트 스크립트
├─ on-pre/       # 온프레미스 환경 구성
├─ aws/          # AWS EKS 환경 구성
├─ cicd/         # CI/CD 관련 구성
└─ docs/         # 문서`

</aside>

### ▷ AWS README

<aside>

**AWS 환경 구성**

**목표**

AWS EKS 기반 Kubernetes 환경 구축 및 운영

**주요 작업**

- VPC 및 네트워크 구성
- EKS 클러스터 구축
- ECR 구성
- RDS PostgreSQL 구성
- AWS Load Balancer Controller 구성
- CloudWatch 모니터링 구성

**디렉토리 설명**

- infra/ : AWS 인프라 구성
- eks/ : EKS 관련 Kubernetes 설정
- monitoring/ : CloudWatch 및 모니터링 구성
- docs/ : AWS 관련 문서

**작업 브랜치**

aws

</aside>

### ▷ ON-PREM README

<aside>

**On-Pre 환경 구성**

**목표**

kubeadm 기반 Kubernetes 온프레미스 환경 구축 및 운영

**주요 작업**

- Ubuntu VM 구성
- Kubernetes 클러스터 구축
- Flannel CNI 구성
- ingress-nginx 구성
- Prometheus / Grafana 모니터링 구성

**디렉토리 설명**

- infra/ : VM 및 네트워크 구성
- k8s/ : Kubernetes 리소스 설정
- monitoring/ : Prometheus / Grafana 구성
- docs/ : 온프레미스 관련 문서

**작업 브랜치**

on-pre

</aside>

### ▷ 공통 규칙

<aside>

**협업 규칙**

**브랜치 전략**

```
main     → 최종 안정 버전
develop  → 통합 테스트 브랜치

aws      → AWS 환경 작업
on-pre   → 온프레미스 환경 작업
cicd     → CI/CD 작업
```

---

**작업 흐름**

**1. 작업 전 최신 상태 가져오기**

```
git checkout 본인브랜치
git pull origin develop
```

---

**2. 작업 후 Commit & Push**

```
git add .
git commit -m "작업 내용 요약"
git push origin 본인브랜치
```

---

**3. Pull Request 생성**

```
본인 브랜치 → develop
```

통합 테스트 후:

```
develop → main
```

---

**폴더 작업 규칙**

**AWS 팀**

- `aws/` 폴더 중심 작업

**On-Pre 팀**

- `on-pre/` 폴더 중심 작업

**CI/CD 팀**

- `cicd/` 폴더 중심 작업

**공통**

- `app/` : 공통 애플리케이션 코드
- `database/` : DB 관련 파일
- `load-test/` : 부하 테스트 관련 파일
- `docs/` : 프로젝트 문서

---

**커밋 메시지 예시**

```
git commit -m "AWS EKS 구조 추가"
git commit -m "on-pre monitoring 설정 추가"
git commit -m "CI/CD 초기 구조 작성"
git commit -m "README 수정"
```

---

**주의사항**

- main 브랜치에 직접 push 금지
- 작업 전 develop 최신화 필수
- 다른 팀 담당 폴더 임의 수정 금지
- 민감 정보(.env, key 등) 업로드 금지
</aside>

### ▷ CICD README

```markdown
# CI/CD 구성

## 목표

온프레미스(on-pre) 환경과 AWS 환경의 배포 자동화 구조를 설계합니다.

현재 CI/CD 도구는 확정하지 않았으며, Jenkins와 GitHub Actions 중 프로젝트 상황에 맞는 방식을 검토합니다.

---

## 주요 작업| Service | Port | GHCR Image |

- 애플리케이션 빌드 자동화
- Docker 이미지 생성 자동화
- 온프레미스 환경 배포 자동화
- AWS EKS 환경 배포 자동화
- 브랜치 기반 배포 흐름 정리
- 배포 실패 시 롤백 방식 검토

---

## 검토 예정 도구

- Jenkins
- GitHub Actions
- Docker Registry
- AWS ECR
- kubectl
- Helm
- GitHub Container Registry (GHCR)

---

## 디렉토리 역할

이 디렉토리는 CI/CD 관련 설정 파일, 파이프라인 스크립트, 배포 자동화 문서를 관리합니다.

도구가 확정되기 전까지는 구체적인 Jenkinsfile이나 workflow 파일을 생성하지 않습니다.

수정 

현재 CI/CD는 GitHub Actions 기반으로 구성하며,

Docker 이미지는 GitHub Container Registry(GHCR)를 사용하여 관리한다.

---

## 작업 브랜치

## 서비스별 포트 및 Container Registry 규칙

GitHub Actions, Docker Build, AWS ECR, Kubernetes 배포 시
서비스명을 통일하기 위해 아래 규칙을 사용한다.

| Service | Port | GHCR Image |
|---|---:|---|
| frontend | 3000 | shoply-frontend |
| gateway | 4000 | shoply-gateway |
| product | 4001 | shoply-product |
| inventory | 4002 | shoply-inventory |
| order | 4003 | shoply-order |
| payment | 4004 | shoply-payment |
| user | 4005 | shoply-user |

GitHub Actions, Docker Build, AWS ECR, Kubernetes 배포 시
서비스명을 통일하기 위해 아래 규칙을 사용한다.

## 브랜치 전략

본 프로젝트는 온프레미스 Kubernetes 환경과 AWS EKS 환경을 비교 실험하는 구조이므로,
작업 영역별 브랜치를 분리하여 관리한다.

## CI/CD 흐름

본 프로젝트는 GitHub Actions를 사용하여 Docker 이미지를 빌드하고,
GitHub Container Registry(GHCR)에 이미지를 저장한다.

온프레미스 Kubernetes 환경과 AWS EKS 환경은
동일한 GHCR 이미지를 Pull하여 실행하는 구조를 사용한다.

```text
Code Push
→ GitHub Actions
→ Docker Build
→ GHCR Push
→ Kubernetes 배포
   ├── on-pre Kubernetes
   └── AWS EKS
   
### 기본 브랜치

- `main`: 최종 안정 버전
- `develop`: 통합 테스트 브랜치

### 작업 브랜치

- `on-pre`: 온프레미스 
- `aws`: AWS 관련 작업
- `cicd`: GitHub Actions, Docker Build, ECR Push, 배포 자동화 관련 작업

### 작업 흐름

```text
on-pre → develop
aws → develop
cicd → develop

develop → main

## GitHub Actions Workflow 구성 계획

서비스별 Docker 이미지 빌드 및 배포 관리를 위해 GitHub Actions workflow를 서비스 단위로 분리한다.

현재는 app 코드와 Dockerfile이 확정되지 않았으므로 `docker-build-test.yml`만 활성화한다.  
각 서비스의 실제 Repo 내부 경로와 Dockerfile 위치가 확정되면 아래 workflow를 순차적으로 추가한다.

| Workflow 파일 | 대상 서비스 | ECR Repository | 상태 |
|---|---|---|---|
| docker-build-test.yml | 공통 테스트 | - | 활성화 |
| gateway.yml | gateway | shoply-gateway | 예정 |
| frontend.yml | frontend | shoply-frontend | 예정 |
| product.yml | product | shoply-product | 예정 |
| inventory.yml | inventory | shoply-inventory | 예정 |
| order.yml | order | shoply-order | 예정 |
| payment.yml | payment | shoply-payment | 예정 |
| user.yml | user | shoply-user | 예정 |
```

- 5/22 수정

```markdown
# CI/CD 구성

## 목표

온프레미스(on-pre) Kubernetes 환경과 AWS EKS 환경의 배포 자동화 구조를 설계한다.

본 프로젝트는 Jenkins와 GitHub Actions를 비교 검토한 뒤,
현재는 GitHub Actions 기반으로 CI/CD를 구성한다.

Docker 이미지는 GitHub Container Registry(GHCR)를 사용하여 관리하며,
온프레미스 Kubernetes와 AWS EKS가 동일한 이미지를 Pull하여 실행하는 구조를 목표로 한다.

---

## 주요 작업

- 애플리케이션 빌드 자동화
- Docker 이미지 생성 자동화
- GitHub Container Registry(GHCR) 이미지 Push 자동화
- 온프레미스 Kubernetes 환경 배포 자동화
- AWS EKS 환경 배포 자동화
- 브랜치 기반 CI/CD 흐름 정리
- 배포 실패 시 롤백 방식 검토

---

## 사용 및 검토 도구

- GitHub Actions
- Docker
- GitHub Container Registry (GHCR)
- Kubernetes
- AWS EKS
- kubectl
- Helm
- Jenkins
- AWS ECR

> Jenkins와 AWS ECR은 초기 검토 대상이었으나,
> 현재 프로젝트에서는 GitHub Actions와 GHCR을 우선 사용한다.

---

## 디렉토리 역할

이 디렉토리는 CI/CD 관련 설정 파일,
GitHub Actions workflow,
파이프라인 스크립트,
배포 자동화 문서를 관리한다.

현재는 GitHub Actions 기반의 Docker Build 및 GHCR Push 흐름을 우선 구성한다.

---

## 서비스별 포트 및 GHCR 이미지 규칙

GitHub Actions, Docker Build, GHCR, Kubernetes 배포 시
서비스명을 통일하기 위해 아래 규칙을 사용한다.

| Service | Port | GHCR Image |
|---|---:|---|
| frontend | 3000 | ghcr.io/ktk026/shoply-frontend |
| gateway | 4000 | ghcr.io/ktk026/shoply-gateway |
| product | 4001 | ghcr.io/ktk026/shoply-product |
| inventory | 4002 | ghcr.io/ktk026/shoply-inventory |
| order | 4003 | ghcr.io/ktk026/shoply-order |
| payment | 4004 | ghcr.io/ktk026/shoply-payment |
| user | 4005 | ghcr.io/ktk026/shoply-user |

---

## 브랜치 전략

본 프로젝트는 온프레미스 Kubernetes 환경과 AWS EKS 환경을 비교 실험하는 구조이므로,
작업 영역별 브랜치를 분리하여 관리한다.

### 기본 브랜치

- `main`: 최종 안정 버전
- `develop`: 통합 테스트 브랜치

### 작업 브랜치

- `on-pre`: 온프레미스 Kubernetes 관련 작업
- `aws`: AWS EKS 및 인프라 관련 작업
- `cicd`: GitHub Actions, Docker Build, GHCR Push, 배포 자동화 관련 작업

### 작업 흐름

```text
on-pre → develop
aws → develop
cicd → develop

develop → main
```

---

## CI/CD 흐름

본 프로젝트는 GitHub Actions를 사용하여 Docker 이미지를 빌드하고,
GitHub Container Registry(GHCR)에 이미지를 저장한다.

온프레미스 Kubernetes 환경과 AWS EKS 환경은
동일한 GHCR 이미지를 Pull하여 실행하는 구조를 사용한다.

```text
Code Push
→ GitHub Actions
→ Docker Build
→ Container 실행 테스트
→ Health Check
→ GHCR Push
→ Kubernetes 배포
   ├── on-pre Kubernetes
   └── AWS EKS
```

---

## 현재 활성화된 Workflow

현재는 `docker-build-test.yml` workflow를 우선 활성화한다.

해당 workflow는 Gateway 서비스를 기준으로 Docker 이미지가 정상적으로 빌드되고,
컨테이너 실행 및 Health Check가 가능한지 검증한 뒤 GHCR에 이미지를 Push한다.

### 현재 테스트 대상

| 항목 | 내용 |
|---|---|
| 대상 서비스 | gateway |
| Dockerfile 경로 | `app/shoply/gateway/Dockerfile` |
| 이미지 이름 | `ghcr.io/ktk026/shoply-gateway` |
| 테스트 태그 | `cicd-test` |
| 서비스 포트 | 4000 |
| Health Check | `/health` |

---

## GitHub Actions Workflow 구성 계획

서비스별 Docker 이미지 빌드 및 배포 관리를 위해
GitHub Actions workflow를 서비스 단위로 분리할 계획이다.

현재는 Gateway 서비스 기준으로 Docker Build,
컨테이너 실행 테스트,
Health Check,
GHCR Push 흐름을 먼저 검증한다.

이후 각 서비스의 Dockerfile 및 Kubernetes 배포 구조가 확정되면
workflow를 서비스 단위로 순차적으로 추가한다.

| Workflow 파일 | 대상 서비스 | GHCR Image | 상태 |
|---|---|---|---|
| docker-build-test.yml | gateway 테스트 | ghcr.io/ktk026/shoply-gateway | 활성화 |
| gateway.yml | gateway | ghcr.io/ktk026/shoply-gateway | 예정 |
| frontend.yml | frontend | ghcr.io/ktk026/shoply-frontend | 예정 |
| product.yml | product | ghcr.io/ktk026/shoply-product | 예정 |
| inventory.yml | inventory | ghcr.io/ktk026/shoply-inventory | 예정 |
| order.yml | order | ghcr.io/ktk026/shoply-order | 예정 |
| payment.yml | payment | ghcr.io/ktk026/shoply-payment | 예정 |
| user.yml | user | ghcr.io/ktk026/shoply-user | 예정 |

---

## AWS EKS 배포 시 이미지 사용 예시

AWS EKS에서는 Deployment YAML의 container image 항목에
GHCR 이미지를 지정하여 배포한다.

```yaml
containers:
  - name: gateway
    image: ghcr.io/ktk026/shoply-gateway:cicd-test
    ports:
      - containerPort: 4000
```

---

## 향후 작업

- GHCR 이미지 Public/Private 정책 결정
- Private 이미지 사용 시 Kubernetes `imagePullSecret` 구성
- 서비스별 Dockerfile 정리
- 서비스별 GitHub Actions workflow 추가
- Kubernetes Deployment / Service YAML 연동
- on-pre Kubernetes와 AWS EKS 배포 방식 비교
- 배포 실패 시 Rollback 방식 정리
```

# ▶︎ GitHub Actions

## ▷ GitHub Actions 동작 테스트용

```
name: Docker Build Test  ##Actions 탭에서 보이는 이름 

on:
  push:
    branches:
      - develop   ##develop 브랜치에서 push 되면 실행
  pull_request:
    branches:
      - develop    ##develop 대상으로 PR 생성 시 실행   

jobs:
  check:
    runs-on: ubuntu-latest     ##실행 환경 GitHub Linux 서버에서 실행 

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4    ##GitHub repo 코드 다운로드 

      - name: Show repository files
        run: |
          pwd
          ls -la    ##현재 repo 구조 확인 : 디버깅에 좋음 

      - name: Check Docker version
        run: docker --version    ##GitHub Actions 서버에 Docker 있는지 확인 

      # app 담당자가 실제 Dockerfile 경로를 확정하면 아래 주석을 해제해서 사용
      # - name: Build gateway image
      #   run: |
      #     docker build -t shoply-gateway:test ./app/msa_shoply/gateway
      ## CI/CD 흐름

본 프로젝트는 동일한 Docker 이미지를 AWS ECR에 저장하고,
온프레미스 Kubernetes와 AWS EKS가 같은 이미지를 Pull하여 실행하는 구조를 사용한다.

```text
Code Push
→ GitHub Actions
→ Docker Build
→ AWS ECR Push
→ Kubernetes 배포
   ├── on-pre Kubernetes
   └── AWS EKS
  
```

- 수정

```yaml
name: Docker Build and Push Test  ## Actions 탭에서 보이는 이름

on:
  push:
    branches:
      - cicd      ## cicd 브랜치에 push 되면 실행
  pull_request:
    branches:
      - develop   ## develop 대상으로 PR 생성 시 실행

jobs:
  check:
    runs-on: ubuntu-latest      ## GitHub Actions Linux 서버에서 실행

    permissions:
      contents: read            ## repository 코드 읽기 권한
      packages: write           ## GHCR에 Docker 이미지 push 권한

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4    ## GitHub repo 코드 다운로드

      - name: Show repository files
        run: |
          pwd
          ls -la                    ## 현재 repo 구조 확인

      - name: Check Docker version
        run: docker --version        ## Docker 설치 여부 확인

      - name: Build gateway image
        run: |
          docker build -t shoply-gateway:test ./app/shoply/gateway
          ## Gateway Docker 이미지 빌드

      - name: Run gateway container
        run: |
          docker run -d --name gateway-test -p 4000:4000 shoply-gateway:test
          docker ps
          ## 빌드한 이미지를 컨테이너로 실행

      - name: Health check
        run: |
          sleep 5
          curl -f http://localhost:4000/health
          ## Gateway health check 확인

      - name: Login to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          ## GitHub 기본 토큰으로 GHCR 로그인

      - name: Tag and push image to GHCR
        run: |
          docker tag shoply-gateway:test ghcr.io/${{ github.repository_owner }}/shoply-gateway:cicd-test
          docker push ghcr.io/${{ github.repository_owner }}/shoply-gateway:cicd-test
          ## GHCR에 Docker 이미지 push
```

- 테스트

```jsx
##테스트용 수정 
echo "GHCR test" >> README.md

##commit and push
git add .
git commit -m "GHCR 테스트"
git push origin cicd

##Git Actions 확인
##GitHub Packages 확인

```

![스크린샷 2026-05-22 오후 11.17.24.png](%EC%9E%91%EC%97%85%EB%82%B4%EC%9A%A9/%E1%84%89%E1%85%B3%E1%84%8F%E1%85%B3%E1%84%85%E1%85%B5%E1%86%AB%E1%84%89%E1%85%A3%E1%86%BA_2026-05-22_%E1%84%8B%E1%85%A9%E1%84%92%E1%85%AE_11.17.24.png)

![스크린샷 2026-05-22 오후 11.18.05.png](%EC%9E%91%EC%97%85%EB%82%B4%EC%9A%A9/%E1%84%89%E1%85%B3%E1%84%8F%E1%85%B3%E1%84%85%E1%85%B5%E1%86%AB%E1%84%89%E1%85%A3%E1%86%BA_2026-05-22_%E1%84%8B%E1%85%A9%E1%84%92%E1%85%AE_11.18.05.png)

## ▷ worlflow 수정

- 이전 workflow: cicd push → Actions 실행
현재 workflow: develop push → Actions 실행

```jsx
name: Docker Build Test

on:
  push:
    branches:
      - develop          ##브랜치cicd 에서 develop으로 변경
  pull_request:
    branches:
      - develop

jobs:
  build-test-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Show repository files
        run: |
          pwd
          ls -la

      - name: Check Docker version 
        run: |                         ##Docker Compose 버전 확인추가 
          docker --version  
          docker compose version

      - name: Build gateway image
        run: |
          docker build -t shoply-gateway:test ./app/shoply/gateway

      - name: Run gateway container
        run: |
          docker run -d --name gateway-test -p 4000:4000 shoply-gateway:test
          docker ps

      - name: Health check
        run: |
          sleep 5
          curl -f http://localhost:4000/health

      - name: Login to GHCR
        if: github.event_name == 'push'
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Tag and push image to GHCR
        if: github.event_name == 'push'   ##PR 생성시 이미지 push안함, 실제 develop push일때 배포 
        run: |
          docker tag shoply-gateway:test ghcr.io/${{ github.repository_owner }}/shoply-gateway:develop
          docker push ghcr.io/${{ github.repository_owner }}/shoply-gateway:develop
          
          
          
          
          
```

## ▷ workflow 수정

- push 한번
↓
frontend build/push
gateway build/push
user build/push
product build/push
inventory build/push

```jsx
name: Docker Build and Push

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        service:
          - name: frontend
            image: shoply-frontend
            path: ./app/shoply
          - name: gateway
            image: shoply-gateway
            path: ./app/shoply/gateway
          - name: user
            image: shoply-user
            path: ./app/shoply/services/user
          - name: product
            image: shoply-product
            path: ./app/shoply/services/product
          - name: inventory
            image: shoply-inventory
            path: ./app/shoply/services/inventory

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Check Docker version
        run: |
          docker --version
          docker compose version

      - name: Build Docker image
        run: |
          docker build -t ${{ matrix.image }}:test ${{ matrix.path }}

      - name: Login to GHCR
        if: github.event_name == 'push'
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Tag and push image to GHCR
        if: github.event_name == 'push'
        run: |
          docker tag ${{ matrix.image }}:test ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}:develop
          docker push ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}:develop
```

## ▷ 수정

| 항목 | 수정 |
| --- | --- |
| 브랜치 | `develop`, `cicd` 둘 다 추가 추천 |
| matrix 참조 | `matrix.service.image`, `matrix.service.path` |
| health check | gateway만 추가 |
| GHCR push | 유지 |

```jsx
name: Docker Build and Push

on:
  push:
    branches:
      - develop
      - cicd
  pull_request:
    branches:
      - develop

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    strategy:
      fail-fast: false
      matrix:
        service:
          - name: frontend
            image: shoply-frontend
            path: ./app/shoply
          - name: gateway
            image: shoply-gateway
            path: ./app/shoply/gateway
          - name: user
            image: shoply-user
            path: ./app/shoply/services/user
          - name: product
            image: shoply-product
            path: ./app/shoply/services/product
          - name: inventory
            image: shoply-inventory
            path: ./app/shoply/services/inventory

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set repository owner to lowercase
        run: |
          echo "OWNER_LC=${OWNER,,}" >> $GITHUB_ENV
        env:
          OWNER: ${{ github.repository_owner }}

      - name: Check Docker version
        run: |
          docker --version
          docker compose version

      - name: Build Docker image
        run: |
          docker build -t ${{ matrix.service.image }}:test ${{ matrix.service.path }}

      - name: Test gateway health check
        if: matrix.service.name == 'gateway'
        run: |
          docker run -d --name gateway-test -p 4000:4000 ${{ matrix.service.image }}:test
          sleep 5
          docker ps
          curl -f http://localhost:4000/health

      - name: Cleanup gateway test container
        if: matrix.service.name == 'gateway' && always()
        run: |
          docker rm -f gateway-test || true

      - name: Login to GHCR
        if: github.event_name == 'push'
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Tag and push image to GHCR
        if: github.event_name == 'push'
        run: |
          BRANCH_TAG=${{ github.ref_name }}
          docker tag ${{ matrix.service.image }}:test ghcr.io/${{ env.OWNER_LC }}/${{ matrix.service.image }}:${BRANCH_TAG}
          docker push ghcr.io/${{ env.OWNER_LC }}/${{ matrix.service.image }}:${BRANCH_TAG}
```

# ▶︎ Docker (아직 X)

## ▷ 도커 이미지

GitHub Packages (GHCR) 

GitHub Actions CI/CD와 연결이 쉽고, 코드 저장소와 이미지 버전 관리가 같이 됨

<aside>

GitHub Actions + Docker + AWS

GitHub Repository
↓ push / PR
GitHub Actions
↓ docker build
GHCR(GitHub Container Registry)
↓ image pull
AWS ECS Fargate 또는 EC2

</aside>

<aside>

GitHub Actions
↓
Docker Build
↓
AWS ECR Push
↓
EKS Deployment

</aside>

## ▷ 도커 이미지 테스트

- 현재 이미지 형식
    - [ghcr.io/ktk026/shoply-gateway:develop](http://ghcr.io/ktk026/shoply-gateway:develop)

## ▷ 도커 컨테이너화 작업

- 사용버전
    
    
    | 항목 | 버전 |
    | --- | --- |
    | Docker Engine | 29.3.1 |
    | Docker Compose | v5.1.1 |
    | Node | 22-alpine |
    | PostgreSQL | 16-alpine |
    | Redis | 7-alpine |

- 이미지 생성 완료
    - frontend
        - 포트:8080
        - 실행
        
        `docker build -t shoply-frontend:test ./app/shoply`
        `docker run -d --name frontend -p 8080:8080 shoply-frontend:test`
        
    - gateway
        - 포트: 4000
        - 실행
        
        `docker build -t shoply-gateway:test ./app/shoply/gateway`
        `docker run -d --name gateway -p 4000:4000 shoply-gateway:test`
        
        - Health Check: `curl [http://localhost:4000/health](http://localhost:4000/health)`
    - user-service
        - 포트: 4005
        - 실행
        
        `docker build -t shoply-user:test .`
        `docker run -d --name user -p 4005:4005 shoply-user:test`
        
    - product-service
        - 포트 : 4001
        - 실행
        
        `docker build -t shoply-product:test .`
        `docker run -d --name product -p 4001:4001 shoply-product:test`
        
    - inventory-service
        - 포트: 4002
        - 실행
        
        `docker build -t shoply-inventory:test .`
        `docker run -d --name inventory -p 4002:4002 shoply-inventory:test`
        

# ▶︎ CICD 서버

- EC2 인스턴스 t3.medium

<aside>

Git pull
Docker image build
Registry push
kubectl apply

</aside>

# ▶︎ ECR 생성

## ▷ ECR 생성

- AWS ECR 프라이빗 레지스트리
    - shoply-frontend
    - shoply-gateway
    - shoply-product
    - shoply-inventory
    - shoply-order
    - shoply-payment
    - shoply-user

# ▶︎ k6

## ▷ k6 test

load-test/k6/gateway-healyh-test.js

```yaml
import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
  vus: 10,
  duration: '30s',
  thresholds: {
    http_req_failed: ['rate<0.01'],
    http_req_duration: ['p(95)<500'],
  },
};

export default function () {
  const res = http.get('http://localhost:4000/health');

  check(res, {
    'status is 200': (r) => r.status === 200,
    'service is gateway': (r) => r.body.includes('gateway'),
  });

  sleep(1);
}
```

## ▷ 부하테스트 시나리오

## 시나리오 1 — 일반 사용자 흐름

**왜 하냐?**

온프레미스가 항상 나쁘다는 식으로 보이면 실험 신뢰도가 떨어집니다.

그래서 먼저:

```
평상시 트래픽에서는 온프레미스도 문제없이 운영 가능하다
```

를 보여줘야 합니다.

**보여줄 것**

```
온프레미스 / EKS 둘 다
- Error Rate 거의 0%
- P95 Latency 안정
- Pod 수 변화 거의 없음
- CPU 여유 있음
```

**발표 메시지**

```
트래픽이 예측 가능하고 안정적이면 온프레미스도 충분합니다.
```

---

## 시나리오 2 — 동시 예약/주문 폭주

**왜 하냐?**

이 프로젝트의 메인 장면입니다.

타임세일처럼 갑자기 요청이 몰리면 온프레미스는 워커노드 자원이 고정되어 있습니다.

반면 EKS는 HPA + Karpenter로 Pod와 Node를 확장할 수 있습니다.

**보여줄 것**

```
온프레미스:
- HPA가 Pod를 늘리려고 함
- 하지만 노드 자원 부족
- Pending Pod 증가
- Error Rate 증가
- Latency 상승

EKS:
- HPA가 Pod 증가
- Karpenter가 Node 추가
- Pending Pod 해소
- Error Rate 낮게 유지
```

**발표 메시지**

```
여기서 운영 방식의 차이가 드러납니다.
```

---

## 시나리오 3 — 점진적 부하 증가

**왜 하냐?**

시나리오 2는 “갑자기 터지는 상황”이고,

시나리오 3은 “한계점이 어디인지” 찾는 실험입니다.

즉:

```
온프레미스는 몇 RPS부터 무너지나?
EKS는 어느 시점에 노드를 추가하나?
```

를 보여줍니다.

**보여줄 것**

```
- RPS 증가 구간
- CPU 70~80% 도달 시점
- HPA 반응 시점
- Pending Pod 발생 시점
- Error Rate 발생 시점
- EKS Node Count 증가 시점
```

**발표 메시지**

```
성능 차이보다 중요한 것은, 한계에 도달했을 때 자동으로 대응할 수 있느냐입니다.
```

---

## 시나리오 4 — 장애 복구

**왜 하냐?**

운영 효율성 비교 프로젝트라면 성능만 보면 부족합니다.

장애가 났을 때 복구 속도까지 봐야 합니다.

예를 들어 워커노드 2를 죽이면:

```
프론트, Order, Payment, User Service Pod가 영향받음
```

문서에서도 워커노드 2에는 프론트, Order, Payment, User를 배치한다고 되어 있습니다.

**보여줄 것**

```
온프레미스:
- 죽은 Pod 재스케줄
- 남은 노드 자원 부족
- Pending 증가
- 복구 지연
- 주문/결제 실패 증가

EKS:
- Karpenter 신규 노드 추가
- Pod 재배치
- 복구 시간 단축
- 실패 건수 최소화
```

**발표 메시지**

```
복구 시간이 곧 비즈니스 손실입니다.
```

---

## 정리하면 시나리오 4개는 역할이 다릅니다

```
1. 일반 사용자 흐름
→ 기준선 만들기

2. 동시 예약/주문 폭주
→ 확장성 차이 보여주기

3. 점진적 부하 증가
→ 한계점과 자동 대응 시점 보여주기

4. 장애 복구
→ 운영 복구력 차이 보여주기
```

## ▷ k6 폴더생성

```yaml
mkdir -p load-test/k6
touch load-test/k6/config.js
touch load-test/k6/scenario-1-normal-flow.js
touch load-test/k6/scenario-2-spike-order.js
touch load-test/k6/scenario-3-ramp-up.js
touch load-test/k6/scenario-4-failure-recovery.js
touch load-test/k6/README.md
```

- load-test/k6/config.js

```yaml
export const BASE_URL = __ENV.BASE_URL || 'http://localhost:4000';

export const FIXED_TOKEN = __ENV.FIXED_TOKEN || '';

export const COMMON_THRESHOLDS = {
  http_req_failed: ['rate<0.05'],
  http_req_duration: ['p(95)<1000'],
};

export const DEFAULT_HEADERS = {
  'Content-Type': 'application/json',
};

export function authHeaders() {
  return {
    ...DEFAULT_HEADERS,
    Authorization: `Bearer ${FIXED_TOKEN}`,
  };
}

```

## ▷ 시나리오 README

<aside>

# k6 Load Test Scenarios

## 개요

본 디렉토리는 온프레미스 Kubernetes 환경과 AWS EKS 환경을 동일 조건으로 비교하기 위한 k6 부하테스트 시나리오를 관리한다.

이번 프로젝트의 핵심 목표는 다음과 같다.

> 동일한 애플리케이션, 동일한 트래픽 조건에서 온프레미스 k8s와 AWS EKS의 운영 특성 차이를 비교한다.
> 

따라서 다음 조건을 동일하게 유지한다.

- 동일한 Docker 이미지
- 동일한 k6 스크립트
- 동일한 RPS 기준
- 동일한 실행 시간
- 동일한 API 비율
- 동일한 DB 스키마
- 동일한 Prometheus/Grafana 지표

단, RPS 값은 초기 기준값이며 실제 부하 테스트 결과에 따라 조정한다.

---

## 디렉토리 구조

```
load-test/
└── k6/
    ├── config.js
    ├── scenario-1-normal-flow.js
    ├── scenario-2-spike-order.js
    ├── scenario-3-ramp-up.js
    ├── scenario-4-failure-recovery.js
    └── README.md
```

---

## 공통 실행 방식

모든 시나리오는 `BASE_URL` 환경변수를 기준으로 실행 대상을 변경한다.

즉, 스크립트는 동일하게 유지하고 테스트 대상 주소만 변경한다.

### 온프레미스 테스트

```bash
BASE_URL=http://<ONPREM-LOADBALANCER-IP> \\
k6 run scenario-1-normal-flow.js
```

### AWS EKS 테스트

```bash
BASE_URL=http://<EKS-ALB-DNS> \\
k6 run scenario-1-normal-flow.js
```

---

## 고정 토큰 사용 방식

시나리오 2, 3은 로그인 병목을 제거하기 위해 고정 JWT 토큰을 사용한다.

목적은 인증 성능 측정이 아니라, 갑작스러운 트래픽 증가 또는 점진적 부하 증가 상황에서 온프레미스와 EKS가 어떻게 다르게 반응하는지 확인하는 것이다.

### 실행 예시

```bash
BASE_URL=http://<ONPREM-LOADBALANCER-IP> \\
FIXED_TOKEN=<발급받은_JWT_TOKEN> \\
k6 run scenario-2-spike-order.js
```

```bash
BASE_URL=http://<EKS-ALB-DNS> \\
FIXED_TOKEN=<발급받은_JWT_TOKEN> \\
k6 run scenario-2-spike-order.js
```

---

## Docker 기반 실행 방식

로컬 또는 EC2에서 Docker 기반으로 k6를 실행할 수 있다.

```bash
docker run --rm -i \\
  -e BASE_URL=http://<TARGET_URL> \\
  -e FIXED_TOKEN=<발급받은_JWT_TOKEN> \\
  -v $(pwd):/scripts \\
  grafana/k6 run /scripts/scenario-2-spike-order.js
```

Docker 기반 실행을 사용하는 이유는 다음과 같다.

- k6 버전 통일
- 실행 환경 차이 제거
- 로컬 PC 성능 차이 최소화
- 반복 실험 재현성 확보
- 팀원 간 동일 실행 환경 유지

---

## Scenario 1 — 일반 사용자 흐름

### 목적

기준선을 형성하기 위한 시나리오이다.

평상시 트래픽에서는 온프레미스 환경도 충분히 안정적으로 운영 가능함을 먼저 보여준다.

### 핵심 서비스

전체 서비스 고르게 사용한다.

### 핵심 측정 지표

- Error Rate 0%
- TPS 안정
- P95 Latency 안정
- CPU 여유
- Pod 수 변화 없음

### 트래픽 타임라인

| 구간 | RPS | 목적 |
| --- | --- | --- |
| 0~5분 | 100 | 워밍업 |
| 5~15분 | 200 | 정상 트래픽 유지 |
| 15~20분 | 200 | 관찰 |

### API 비율

| 엔드포인트 | 비율 |
| --- | --- |
| GET /api/products | 70% |
| POST /api/orders | 20% |
| POST /api/payments | 10% |

### 보여줄 것

양쪽 환경 모두 다음 상태가 유지되는지 확인한다.

- Error Rate 0%
- P95 Latency 안정
- CPU 여유
- Pod 수 변화 없음

### 메시지

> 트래픽이 예측 가능하고 안정적이면 온프레미스도 충분합니다.
> 

---

## Scenario 2 — 동시 예약/주문 폭주

### 목적

갑자기 트래픽이 터졌을 때 온프레미스와 AWS EKS가 어떻게 반응하는지 비교한다.

시나리오 2는 로그인 성능을 측정하는 것이 아니다.

따라서 고정 JWT 토큰을 사용하여 로그인 병목을 제거하고, 주문/결제/조회 부하에 집중한다.

### 핵심 비교 포인트

- 온프레미스: Pod CPU 증가 → HPA 파드 추가 시도 → 노드 자원 부족 → Pending 증가 → Error Rate 급등
- EKS: Pod CPU 증가 → HPA 파드 추가 시도 → Karpenter 노드 자동 추가 → Pending 해소 → Error Rate 안정화

### 집중 노드

워커노드 1

- API Gateway
- Product Service
- Inventory Service

### 핵심 서비스

- Product Service
- Inventory Service
- Order Service
- Payment Service

### 핵심 측정 지표

- Pending Pod
- Node Count
- Error Rate
- HPA desired vs current
- P95 Latency

### 트래픽 타임라인

| 구간 | RPS | 목적 |
| --- | --- | --- |
| 0~5분 | 100 | 정상 트래픽 |
| 5분 | 1000 | 순간 급증 |
| 5~15분 | 1000 | 폭증 유지 |
| 15~20분 | 1000 | 안정화 관찰 |

### API 비율

| 엔드포인트 | 비율 |
| --- | --- |
| GET /api/products | 50% |
| POST /api/orders | 30% |
| POST /api/payments | 20% |

### 예상 결과

| 환경 | 예상 결과 |
| --- | --- |
| 온프레미스 | HPA Pod 증설 시도 → 노드 자원 부족 → Pending 증가 → Error Rate 급증 |
| EKS | HPA Pod 증설 → Karpenter 노드 자동 추가 → Pending 해소 → Error Rate 안정화 |

### 보여줄 것

- Pending Pod 타임라인
- Node Count 자동 증가
- Error Rate 차이
- HPA desired/current 괴리

### 메시지

> 여기서 운영 방식의 차이가 드러납니다.
> 

---

## Scenario 3 — 점진적 부하 증가

### 목적

몇 RPS부터 온프레미스가 한계에 도달하는지 숫자로 확인한다.

시나리오 2가 “트래픽이 갑자기 터지는 상황”을 보여준다면, 시나리오 3은 “몇 RPS에서 한계가 발생하는지”를 데이터로 증명한다.

시나리오 3 역시 고정 JWT 토큰을 사용한다.

목적은 로그인 병목이 아니라, 트래픽 증가에 대한 인프라 반응 차이를 확인하는 것이다.

### 핵심 측정 지표

- CPU 70~80% 도달 시점
- HPA 반응 시점
- Pending Pod 발생 시점
- Error Rate 발생 시점
- Node Count 증가 시점

### 트래픽 타임라인

| 구간 | RPS | 목적 |
| --- | --- | --- |
| 0~2분 | 100 | 기준 |
| 2~4분 | 300 | 증가 |
| 4~6분 | 500 | 임계점 탐색 |
| 6~8분 | 700 | 부하 심화 |
| 8~10분 | 1000 | 최대 부하 |
| 10~20분 | 1000 | 유지 및 관찰 |

### API 비율

| 엔드포인트 | 비율 |
| --- | --- |
| GET /api/products | 50% |
| POST /api/orders | 30% |
| POST /api/payments | 20% |

### 예상 결과

| 환경 | 예상 결과 |
| --- | --- |
| 온프레미스 | 특정 RPS 구간에서 CPU 급등 → HPA 발동 → Pending → Error Rate 발생 |
| EKS | HPA 발동 → Karpenter 노드 추가 → Pending 없이 계속 확장 |

### 보여줄 것

- RPS 증가 구간별 CPU 변화
- Error Rate 발생 시점
- Pending Pod 발생 시점
- EKS Node Count 증가 타이밍

### 메시지

> 성능 차이보다 중요한 것은, 한계에 도달했을 때 자동으로 대응할 수 있느냐입니다.
> 

---

## Scenario 4 — 장애 복구

### 목적

노드 장애 발생 시 서비스가 언제 다시 정상화되는지 확인한다.

이 시나리오는 실제 사용자 흐름을 재현하기 위해 로그인 요청을 포함한다.

사용자는 장애 상황에서도 계속 로그인하고, 상품을 조회하고, 주문하고, 결제를 시도한다.

### 장애 상황

워커노드 2 강제 종료

워커노드 2에 배치된 서비스:

- Frontend
- Order Service
- Payment Service
- User Service

장애 발생 시 상품 조회는 가능하지만 주문/결제 흐름이 실패할 수 있다.

### 핵심 측정 지표

- MTTR
- 결제 실패 건수
- Pending Pod
- Pod 재스케줄링 시간
- 데이터 정합성
- Node Count 변화

### 트래픽 타임라인

| 구간 | RPS | 목적 |
| --- | --- | --- |
| 0~5분 | 500 | 실제 사용자 흐름 |
| 5분 | - | 워커노드 2 강제 종료 |
| 5~15분 | 500 | 복구 관찰 |
| 15~20분 | 500 | 정상 확인 |

### API 비율

| 엔드포인트 | 비율 |
| --- | --- |
| POST /api/auth/login | 10% |
| GET /api/products | 40% |
| POST /api/orders | 30% |
| POST /api/payments | 20% |

### 예상 결과

| 환경 | 예상 결과 |
| --- | --- |
| 온프레미스 | 죽은 Pod → 워커노드1 재스케줄 시도 → 자원 부족 → Pending → 복구 지연 → 결제 실패 증가 |
| EKS | Karpenter 신규 노드 추가 → Pod 재배치 → 빠른 복구 → 결제 실패 최소화 |

### 보여줄 것

- MTTR 비교
- 장애 중 결제 실패 건수
- Pending Pod 타임라인
- Node Count 변화

### 메시지

> 복구 시간이 곧 비즈니스 손실입니다.
> 

---

## RPS 값 조정 기준

현재 RPS 값은 최종 확정값이 아니라 초기 기준값이다.

실제 부하 테스트를 통해 다음 기준으로 조정한다.

- 온프레미스에서 CPU가 70~80%에 도달하는 구간
- HPA가 반응하기 시작하는 구간
- Pending Pod가 발생하는 구간
- Error Rate가 발생하기 시작하는 구간
- EKS에서 Karpenter가 노드를 추가하는 구간

즉, 중요한 것은 1000 RPS라는 숫자 자체가 아니라 두 환경이 동일 조건에서 어떻게 다르게 반응하는지를 확인하는 것이다.

---

## 핵심 비교 지표

| 지표 | 설명 |
| --- | --- |
| TPS | 초당 처리 요청 수 |
| P95 Latency | 사용자 체감 응답속도 |
| Error Rate | 5xx 에러 비율 |
| Pod Count | Running Pod 수 |
| Pending Pod | 자원 부족 상태 |
| Node Count | 노드 자동 증가 여부 |
| HPA current vs desired | Autoscaling 정상 여부 |
| CPU / Memory | 리소스 사용량 |
| MTTR | 장애 발생 후 복구까지 걸린 시간 |

---

## 핵심 메시지

이번 프로젝트는 단순 성능 비교가 아니라 동일한 환경 조건에서 온프레미스 Kubernetes와 AWS EKS의 운영 특성 차이를 데이터로 비교하는 실험이다.

</aside>

## ▷ 시나리오 1

- load-test/k6/scenario-1-normal-flow.js
    
    ```
    import http from 'k6/http';
    import { check, sleep } from 'k6';
    
    import {
      BASE_URL,
      COMMON_THRESHOLDS,
      DEFAULT_HEADERS,
    } from './config.js';
    
    export const options = {
      scenarios: {
        normal_flow: {
          executor: 'ramping-arrival-rate',
    
          // 시작 요청률
          startRate: 100,
    
          // 초당 요청 기준
          timeUnit: '1s',
    
          // 미리 확보할 VU
          preAllocatedVUs: 100,
    
          // 최대 VU
          maxVUs: 300,
    
          // 20분 시나리오
          stages: [
            // 0~5분
            { target: 100, duration: '5m' },
    
            // 5~15분
            { target: 200, duration: '10m' },
    
            // 15~20분
            { target: 200, duration: '5m' },
          ],
        },
      },
    
      thresholds: COMMON_THRESHOLDS,
    };
    
    export default function () {
      // 1. 로그인
      const loginPayload = JSON.stringify({
        email: 'user1@test.com',
        password: '1234',
      });
    
      const loginRes = http.post(
        `${BASE_URL}/api/auth/login`,
        loginPayload,
        {
          headers: DEFAULT_HEADERS,
        }
      );
    
      check(loginRes, {
        'login success': (r) => r.status === 200,
      });
    
      let token = null;
    
      try {
        token = loginRes.json('token');
      } catch (e) {
        token = null;
      }
    
      // 인증 헤더
      const authHeader = token
        ? {
            ...DEFAULT_HEADERS,
            Authorization: `Bearer ${token}`,
          }
        : DEFAULT_HEADERS;
    
      // 2. 상품 목록 조회
      const productsRes = http.get(
        `${BASE_URL}/api/products`,
        {
          headers: authHeader,
        }
      );
    
      check(productsRes, {
        'products loaded': (r) => r.status === 200,
      });
    
      // 3. 상품 상세 조회
      const productDetailRes = http.get(
        `${BASE_URL}/api/products/1`,
        {
          headers: authHeader,
        }
      );
    
      check(productDetailRes, {
        'product detail loaded': (r) => r.status === 200,
      });
    
      // 사용자 행동 대기
      sleep(1);
    }
    ```
    
     
    
- 개요
    
    
    | **항목** | **상세 내용** | **비고** |
    | --- | --- | --- |
    | **목적** | **기준선(Baseline) 형성** — 평상시 트래픽 규모에서는 온프레미스와 EKS 모두 안정적임을 증명 | 양쪽 모두 정상 작동 확인 |
    | **핵심 서비스** | 전체 마이크로서비스 고르게 부하 분산 (Gateway, Auth, Product, Order, Payment) | 특정 서비스 병목 배제 |
    | **핵심 측정 지표** | Error Rate 0% / TPS 안정성 / CPU 및 Memory 여유 / Pod 수 변화 없음 | 인프라 기초 체력 측정 |
    | **핵심 메시지** | **"트래픽이 예측 가능하고 안정적이면 온프레미스도 충분합니다."** | 핵심 결론 프레이징 |
    
- 부하테스트 실행 가이드
    
    
    | **분류** | **항목** | **설정값 (단위)** | **시나리오 의미 및 목적** |
    | --- | --- | --- | --- |
    | k6
    실행
    옵션 | 동시 접속 수 (vus) | **50** (명) | 동시 접속자 수를 50명으로 고정하여 안정적인 부하 생성 |
    | 총 테스트 시간 (duration) | **20** (분) | 기준선 형성: 평소 서비스 운영 시의 인프라 상태를 데이터로 기록 |  |
    | 목표 요청 수 (RPS) | **100 ➔ 200 ➔ 200** (RPS) | 온프레미스와 EKS 모두 안정적으로 처리 가능한 기본 대역폭 측정 |  |
    | 사용자
    시나리오
    흐름 | ① 인증 (Auth) | POST /api/auth/login | 동적 토큰 발급 및 가상 유저 세션 초기화 (로그인 성공 검증) |
    | ② 목록 조회 (Product) | GET /api/products | 전체 상품 목록 로드 및 인메모리/캐시 레이어 정상 작동 검증 |  |
    | ③ 상세 조회 (Product) | GET /api/products/1 | 특정 상품 단건 조회 (각 요청 단계 후 **1초 대기(sleep(1))**로 유저 행동 모사) |  |
- 트래픽 타임 라인 및 구간별 상세 목적
    
    
    | **구간** | **목표 RPS** | **구간 명칭** | **인프라 관점의 상세 목적 및 검증 요소** |
    | --- | --- | --- | --- |
    | **0 ~ 5분** | **100 RPS** | **워밍업 구간** | 초기부터 높은 부하를 주지 않고 실제 운영 환경과 유사한 상태 조성
    • **Redis 캐시 레이어 예열** (Cache Warming)
    • **DB Connection Pool** 안정화 및 초기화
    • **애플리케이션 및 Pod 초기화** (JVM Warm-up 등) 완료 |
    | **5 ~ 15분** | **200 RPS** | **정상 운영 구간** | 예측 가능한 정상 피크 타임 트래픽 수준을 장시간 지속 유입
    • 지속적인 요청 환경에서의 **서비스 안정성** 및 붕괴 여부 관찰
    • 사용자 체감 **응답 속도(P95 Latency)** 및 **Error Rate 0%** 검증
    • 안정 상태에서의 시스템 **리소스(CPU/Memory) 사용량** 추이 파악 |
    | **15 ~ 20분** | **200 RPS** | **관찰 및 잔여 검증** | 트래픽 지속 상태를 최종 유지하며 인프라 내구성 검증 완료 단계
    • 비즈니스 로직 처리 후 백엔드 **잔여 큐(Queue)** 처리 능력 확인
    • 트래픽 변동이 없을 때 **Pod 수가 스케일링 없이 유지**되는지 검증 |
- API 부하 비율 및 엔드포인트 상세
    
    
    | **엔드포인트** | **메서드** | **목표 비율** | **비고 및 시나리오 의미** |
    | --- | --- | --- | --- |
    | /api/auth/login | **POST** | 10% | 사용자 인증 및 세션 생성 |
    | /api/products | **GET** | **60%** | 단순 상품 검색 및 조회 중심의 흐름 (전체 트래픽의 대다수 모사) |
    |  /api/products/1 | **GET** | **30%** | 특정 상품 상세 페이지 조회 |
- 테스트 최종 목표
    - **Error Rate 0%** 및 **P95 Latency 안정성** 확인 (양쪽 환경 동일)
    - **CPU / Memory 자원의 여유 공간** 확보 상태 증명
    - 트래픽이 자원 한계 미만이므로 **Pod 수 변화 없음 (HPA 미발동)** 상태 기록
    

## ▷ 시나리오 2

- load-test/k6/scenario-2-spike-order.js
    
    ```yaml
    import http from 'k6/http';
    import { check, sleep } from 'k6';
    
    import {
      BASE_URL,
      COMMON_THRESHOLDS,
      authHeaders,
    } from './config.js';
    
    export const options = {
      scenarios: {
        spike_order: {
          executor: 'ramping-arrival-rate',
    
          // 시작 요청률
          startRate: 100,
    
          // 초당 요청 기준
          timeUnit: '1s',
    
          // 미리 확보할 VU
          preAllocatedVUs: 500,
    
          // 최대 VU
          maxVUs: 1500,
    
          // 20분 시나리오
          stages: [
            // 0~5분 정상 트래픽
            { target: 100, duration: '5m' },
    
            // 5분 시점 순간 급증
            { target: 1000, duration: '1s' },
    
            // 5~20분 폭증 유지 및 관찰
            { target: 1000, duration: '15m' },
          ],
        },
      },
    
      thresholds: COMMON_THRESHOLDS,
    };
    
    export default function () {
      const productId = Math.floor(Math.random() * 100) + 1;
      const userId = Math.floor(Math.random() * 1000) + 1;
    
      // 1. 상품 조회
      const productsRes = http.get(`${BASE_URL}/api/products`, {
        headers: authHeaders(),
      });
    
      check(productsRes, {
        'products status is 200': (r) => r.status === 200,
      });
    
      // 2. 주문 생성
      const orderPayload = JSON.stringify({
        userId,
        productId,
        quantity: 1,
      });
    
      const orderRes = http.post(
        `${BASE_URL}/api/orders`,
        orderPayload,
        {
          headers: authHeaders(),
        }
      );
    
      check(orderRes, {
        'order status is 200 or 201': (r) =>
          r.status === 200 || r.status === 201,
      });
    
      let orderId = null;
    
      try {
        orderId = orderRes.json('orderId') || orderRes.json('id');
      } catch (e) {
        orderId = null;
      }
    
      // 3. 결제 요청
      if (orderId) {
        const paymentPayload = JSON.stringify({
          orderId,
          userId,
          amount: 10000,
        });
    
        const paymentRes = http.post(
          `${BASE_URL}/api/payments`,
          paymentPayload,
          {
            headers: authHeaders(),
          }
        );
    
        check(paymentRes, {
          'payment status is 200 or 201': (r) =>
            r.status === 200 || r.status === 201,
        });
      }
    
      sleep(1);
    }
    ```
    
- 시나리오 개요
    
    
    | **항목** | **상세 내용** | **비고** |
    | --- | --- | --- |
    | **목적** | **인프라 확장성(Scalability) 차이 규명** — 갑자기 트래픽이 터졌을 때 HPA와 Karpenter 유무가 비즈니스에 미치는 영향 대조 | 확장성 한계 및 차이 증명 |
    | **핵심 서비스** | 주문 관련 핵심 마이크로서비스 (Gateway, Product, Inventory, Order, Payment) | 코어 비즈니스 자원 압박 |
    | **핵심 측정 지표** | Pending Pod 발생 추이 / Node 자동 추가 소요 시간 / Error Rate 격차 / TPS 드롭 여부 | 인프라 한계 및 복구력 계측 |
    | **핵심 메시지** | **"갑작스러운 트래픽 폭증 상황에서, 운영 방식의 결정적인 차이가 드러납니다."** | 핵심 결론 프레이징 |
- 부하테스트 실행 가이드
    
    
    | **분류** | **항목** | **설정값 (단위)** | **시나리오 의미 및 목적** |
    | --- | --- | --- | --- |
    | k6
    실행
    옵션 | 동시 접속 수 (vus) | **50 ➔ 500** (명) | 5분 시점에 동시 접속자 수를 10배로 늘려 인프라에 순간적인 충격(Spike) 가해 |
    | 총 테스트 시간 (duration) | **20** (분) | 폭증 트래픽 유입 시점부터 인프라가 자가 치유되거나 무너지는 전 과정을 기록 |  |
    | 목표 요청 수 (RPS) | **100 ➔ 1,000 ➔ 1,000** (RPS) | 평시 대역폭에서 타임세일 정각에 트래픽이 10배 폭증하는 상황 완벽 재현 |  |
    | 사용자
    시나리오
    흐름 | ① 고정 토큰 세팅 | Authorization: Bearer <GIVEN_TOKEN> | **고정 토큰(Static Token) 사용:** 인증 서버(Auth)의 로그인 병목을 유도하지 않고, 순수 주문/결제 서비스 성능 검증에만 부하를 집중시킴 |
    | ② 상품 상세 조회 | GET /api/products/1 | 구매 타겟 상품 페이지 진입 및 재고 현황 파악 파이프라인 압박 |  |
    | ③ 주문 및 결제 | POST /api/orders ➔ POST /api/payments | 순간적인 트랜잭션 집중을 통해 인벤토리 차감 및 결제 모듈의 자원 소모 극대화 |  |
- 트래픽 타임라인 및 구간별 상세 목적
    
    
    | **구간** | **목표 RPS** | **구간 명칭** | **인프라 관점의 상세 목적 및 검증 요소** |
    | --- | --- | --- | --- |
    | **0 ~ 5분** | **100 RPS** | **정상 트래픽 구간** | 타임세일 시작 직전의 고요한 정상 상태를 모사하며 이벤트 돌입 전 시스템 자원 안정성 최종 확인 |
    | **5분 시점** | **1,000 RPS** | **순간 급증 (Spike)** | **정각이 되는 순간 트래픽을 10배로 수직 상승시켜 두 환경의 스케일링 메커니즘 검증**
    • **온프레미스:** CPU 급등 ➔ HPA 발동 ➔ 물리 노드 자원 고갈 ➔ 신규 Pod들이 **Pending(대기)** 상태로 멈추며 큐가 밀리고 **Error Rate가 폭증**하는 현상 목격
    • **EKS:** CPU 급등 ➔ HPA 발동 ➔ 가용 자원 고갈 즉시 **Karpenter**가 Pending 상태를 감지하여 AWS EC2 노드를 수십 초 내로 자동 증설하는 연동 과정 관찰 |
    | **5 ~ 20분** | **1,000 RPS** | **폭증 유지 및 관찰** | 폭발적인 대기 트래픽이 연속될 때 인프라의 처리 지속력 및 자동화 내구성 검증
    • **온프레미스:** 자원 확장 불가능으로 인해 에러 상태가 지속되며 시스템이 다운되는 과정 증명
    • **EKS:** 자동 추가된 가상 노드에 Pod들이 정상 배치되면서 **Pending이 해소되고 Error Rate가 다시 0%로 안정화**되는 능력 입증 |
- API 부하 비율 및 엔드포인트 상세
    
    
    | **엔드포인트** | **메서드** | **목표 비율** | **비고 및 시나리오 의미** |
    | --- | --- | --- | --- |
    | /api/products | **GET** | **50%** | 타임세일 오픈과 동시에 상품 상세 페이지를 새로고침하며 진입하는 트래픽 모사 |
    | /api/orders | **POST** | **30%** | 구매하기 버튼을 눌러 동시에 주문 생성을 시도하는 트래픽 (인벤토리 락 유도) |
    | /api/payments | **POST** | **20%** | 결제 요청 단계로 이어지는 고부하 핵심 트랜잭션 압박 |
- 테스트 최종 목표
    - **온프레미스의 한계:** 한정된 하드웨어 자원 내에서 HPA가 Pod을 늘리려 해도 노드가 버텨주지 못해 결국 **Pending Pod이 누적되고 대규모 결제 실패(Error Rate 급등)**가 나는 장면 시각화
    - **EKS + Karpenter의 우수성:** 인프라 자원이 부족해지는 순간, 클라우드가 스스로 판단하여 **새로운 노드를 즉시 붙여 요청을 모두 정상 처리(Error Rate 0% 수렴)**해내는 자동화 역량 입증

## ▷ 시나리오 3

- load-test/k6/scenario-3-ramp-up.js
    
    ```yaml
    import http from 'k6/http';
    import { check, sleep } from 'k6';
    
    import {
      BASE_URL,
      COMMON_THRESHOLDS,
      authHeaders,
    } from './config.js';
    
    export const options = {
      scenarios: {
        ramp_up_load: {
          executor: 'ramping-arrival-rate',
    
          // 시작 요청률
          startRate: 100,
    
          // 초당 요청 기준
          timeUnit: '1s',
    
          // 미리 확보할 VU
          preAllocatedVUs: 500,
    
          // 최대 VU
          maxVUs: 1500,
    
          // 점진적 증가 시나리오
          stages: [
            { target: 100, duration: '2m' },
            { target: 300, duration: '2m' },
            { target: 500, duration: '2m' },
            { target: 700, duration: '2m' },
            { target: 1000, duration: '2m' },
    
            // 최대 부하 유지 및 관찰
            { target: 1000, duration: '10m' },
          ],
        },
      },
    
      thresholds: COMMON_THRESHOLDS,
    };
    
    export default function () {
      const productId = Math.floor(Math.random() * 100) + 1;
      const userId = Math.floor(Math.random() * 1000) + 1;
    
      // 1. 상품 조회
      const productsRes = http.get(`${BASE_URL}/api/products`, {
        headers: authHeaders(),
      });
    
      check(productsRes, {
        'products status is 200': (r) => r.status === 200,
      });
    
      // 2. 주문 생성
      const orderPayload = JSON.stringify({
        userId,
        productId,
        quantity: 1,
      });
    
      const orderRes = http.post(
        `${BASE_URL}/api/orders`,
        orderPayload,
        {
          headers: authHeaders(),
        }
      );
    
      check(orderRes, {
        'order status is 200 or 201': (r) =>
          r.status === 200 || r.status === 201,
      });
    
      let orderId = null;
    
      try {
        orderId = orderRes.json('orderId') || orderRes.json('id');
      } catch (e) {
        orderId = null;
      }
    
      // 3. 결제 요청
      if (orderId) {
        const paymentPayload = JSON.stringify({
          orderId,
          userId,
          amount: 10000,
        });
    
        const paymentRes = http.post(
          `${BASE_URL}/api/payments`,
          paymentPayload,
          {
            headers: authHeaders(),
          }
        );
    
        check(paymentRes, {
          'payment status is 200 or 201': (r) =>
            r.status === 200 || r.status === 201,
        });
      }
    
      sleep(1);
    }
    ```
    

- 시나리오 개요
    
    
    | **항목** | **상세 내용** | **비고** |
    | --- | --- | --- |
    | **목적** | **한계점(Break-point) 탐색 및 계측** — 부하를 단계별로 높여가며 온프레미스가 붕괴하는 정확한 RPS 임계점과 EKS의 유연한 노드 추가 타이밍을 대조 | 인프라 임계치 정밀 계측 |
    | **핵심 서비스** | 전체 마이크로서비스 (Gateway, Product, Inventory, Order, Payment) 고르게 압박 | 전 계층 자원 포화 유도 |
    | **핵심 측정 지표** | CPU 70~80% 도달 RPS / 온프레미스 최초 에러 발생 RPS / EKS 자원 유연 확장 타이밍 | 한계점 데이터 확보 |
    | **핵심 메시지** | **"단순한 성능 차이보다 중요한 것은, 시스템이 한계에 도달했을 때 자동으로 대응할 수 있느냐입니다."** | 핵심 결론 프레이징 |
- 부하 테스트 실행 가이드
    
    
    | **분류** | **항목** | **설정값 (단위)** | **시나리오 의미 및 목적** |
    | --- | --- | --- | --- |
    | k6
    실행
    옵션 | 동시 접속 수 (vus) | **50 ➔ 150 ➔ 250 ➔ 350 ➔ 500** (명) | 트래픽 증가에 맞춰 가상 사용자 수(VU)를 2분 간격으로 계단식 램프업(Ramp-up) |
    | 총 테스트 시간 (duration) | **20** (분) | 10분간의 계단식 부하 상승 구간과 10분간의 최대 부하 유지 구간으로 구성 |  |
    | 목표 요청 수 (RPS) | **100 ➔ 300 ➔ 500 ➔ 700 ➔ 1,000** (RPS) | 서서히 물이 차오르듯 부하를 가해 인프라의 여유 자원이 갉아먹히는 과정을 정밀 추적 |  |
    | 사용자
    시나리오
    흐름 | ① 고정 토큰 세팅 | Authorization: Bearer <GIVEN_TOKEN> | **고정 토큰(Static Token) 사용:** 인증 부하를 격리하여 순수 비즈니스 트랜잭션 증가에 따른 인프라 내구성을 정밀 계측 |
    | ② 상품 검색 및 상세 조회 | GET /api/products ➔ /products/1 | 트래픽 상승에 따른 애플리케이션 캐시 적중률 변화 및 데이터베이스 조회 압박 |  |
    | ③ 점진적 주문/결제 유입 | POST /api/orders ➔ POST /api/payments | 처리량이 한계에 다다랐을 때 연결(Connection)이 맺히지 않고 튕겨 나가는 시점 탐색 |  |
- 트래픽 타임라인 및 구간별 상세 목적
    
    
    | **구간** | **목표 RPS** | **구간 명칭** | **인프라 관점의 상세 목적 및 검증 요소** |
    | --- | --- | --- | --- |
    | **0 ~ 2분** | **100 RPS** | **초기 기준선 구간** | 부하가 본격적으로 증가하기 전, 시스템의 초기 리소스 소모량 변화율 측정 |
    | **2 ~ 4분** | **300 RPS** | **1차 부하 증가** | 완만한 트래픽 상승 단계에서 CPU 및 Memory 자원 소모 흐름 추적 |
    | **4 ~ 6분** | **500 RPS** | **임계점 탐색 단계** | • **온프레미스:** 자원 병목의 전조 증상 관찰 (CPU 사용량 70~80% 돌파 및 HPA 최초 트리거 시점 확인)
    • **EKS:** Pod 증가와 함께 물리 자원 한계 임계치를 예측하는 구간 |
    | **6 ~ 8분** | **700 RPS** | **부하 심화 및 지연 관찰** | 인프라 경고 구간 유입에 따른 전체 서비스의 지연 시간(P95 Latency) 늘어남 및 처리량(Throughput) 꺾임 현상 관찰 |
    | **8 ~ 10분** | **1,000 RPS** | **최대 부하 도달 (한계점 수립)** | 자원 완전 고갈로 인해 온프레미스 인프라에서 **'최초로 Error Rate가 발생하며 무너지는 정확한 RPS 수치'** 기록 및 특정 |
    | **10 ~ 20분** | **1,000 RPS** | **최대 부하 유지 및 내성 검증** | 한계점 트래픽을 장시간 유지하며 두 환경의 자가 치유 및 내구성 관찰
    • **온프레미스:** 추가 확장이 불가능하여 지속적으로 성능이 저하되고 에러가 누적되는 현상 증명
    • **EKS:** 부하 증가에 맞춰 **Karpenter가 필요한 만큼만 노드를 단계별(1대, 2대...)로 유연하게 늘려 대응**하는 타이밍 데이터 확보 |
- API 부하 비율 및 엔드포인트 상세
    
    
    | **엔드포인트** | **메서드** | **목표 비율** | **비고 및 시나리오 의미** |
    | --- | --- | --- | --- |
    | /api/products | **GET** | **50%** | 부하 증가에 따른 단순 상품 검색 및 데이터베이스 동시 읽기(Read) 부하 유도 |
    | /api/orders | **POST** | **30%** | 임계점 도달 시 인벤토리 시스템의 트랜잭션 처리 한계 압박 |
    | /api/payments | **POST** | **20%** | 시스템이 무너질 때 결제 유실이 발생하는 비즈니스 리스크 구체화 |
- 테스트 최종 목표
    - **온프레미스의 한계 특정:** "우리 온프레미스 환경은 딱 OOO RPS부터 무너지기 시작합니다"라는 **정량적인 병목 임계점 숫자를 보고서에 명시**할 수 있는 데이터 확보
    - **EKS의 유연한 비용 효율성:** 무조건 노드를 많이 띄워두는 게 아니라, 계단식 부하에 발맞추어 **Karpenter가 딱 필요한 만큼만(Just-in-time) 노드를 조절하며 늘려가는 유연성** 증명
    

## ▷ 시나리오 4

- load-test/k6/scenario-4-failure-recovery.js
    
    ```yaml
    import http from 'k6/http';
    import { check, sleep } from 'k6';
    
    import {
      BASE_URL,
      COMMON_THRESHOLDS,
      DEFAULT_HEADERS,
    } from './config.js';
    
    export const options = {
      scenarios: {
        failure_recovery: {
          executor: 'constant-arrival-rate',
          rate: 500,
          timeUnit: '1s',
          duration: '20m',
          preAllocatedVUs: 500,
          maxVUs: 1500,
        },
      },
      thresholds: COMMON_THRESHOLDS,
    };
    
    export default function () {
      const productId = Math.floor(Math.random() * 100) + 1;
      const userId = Math.floor(Math.random() * 1000) + 1;
    
      const orderPayload = JSON.stringify({
        userId,
        productId,
        quantity: 1,
      });
    
      const orderRes = http.post(
        `${BASE_URL}/api/orders`,
        orderPayload,
        {
          headers: DEFAULT_HEADERS,
        }
      );
    
      check(orderRes, {
        'order status is 200 or 201': (r) => r.status === 200 || r.status === 201,
      });
    
      let orderId = null;
    
      try {
        orderId = orderRes.json('orderId') || orderRes.json('id');
      } catch (e) {
        orderId = null;
      }
    
      if (orderId) {
        const paymentPayload = JSON.stringify({
          orderId,
          userId,
          amount: 10000,
        });
    
        const paymentRes = http.post(
          `${BASE_URL}/api/payments`,
          paymentPayload,
          {
            headers: DEFAULT_HEADERS,
          }
        );
    
        check(paymentRes, {
          'payment status is 200 or 201': (r) => r.status === 200 || r.status === 201,
        });
      }
    
      sleep(1);
    }
    ```
    
- 시나리오 개요
    
    
    | **항목** | **내용** | **비고** |
    | --- | --- | --- |
    | **목적** | 운영 복구력 — 장애 발생 시 복구 속도와 비즈니스 손실(매출 타격) 차이 증명 | 회복 탄력성 검증 |
    | **장애 대상** | **워커 노드 2 강제 종료 (Power Off)** ➔ Auth(User), Order, Payment Pod 타격 | **일부 코어 서비스 유실** |
    | **핵심 서비스** | Order Service, Payment Service, Auth(User) Service | 비즈니스 핵심 계층 |
    | **핵심 측정 지표** | MTTR(평균 복구 시간) / 장애 시간 내 결제 실패 건수 / Pending Pod 타임라인 / 데이터 정합성 | 손실 규모 정량화 |
    | **핵심 메시지** | **"복구 시간이 곧 비즈니스 손실입니다."** | 핵심 결론 프레이징 |
    
- 부하 테스트 실행 가이드
    
    
    | **분류** | **항목** | **설정값 (단위)** | **시나리오 의미 및 목적** |
    | --- | --- | --- | --- |
    | k6
    실행
    옵션 | 동시 접속 수 (vus) | **250** (명) | 장애 발생 시 부하가 밀리는 현상을 관찰하기 위해 250명의 동시 사용자를 상시 유지 |
    | 총 테스트 시간 (duration) | **20** (분) | 장애 전 정상 상태(5분) ➔ 장애 발발(5분 시점) ➔ 자가 치유 및 관찰(15분) 전 과정 추적 |  |
    | 목표 요청 수 (RPS) | **500** (RPS) | 실제 사용자들이 몰리는 피크 타임 상태를 모사하여 가혹한 장애 환경 연출 |  |
    | 사용자
    시나리오
    흐름 | ① 동적 로그인 인증 | POST /api/auth/login | **실제 사용자 흐름(Dynamic Flow) 사용:** 매번 로그인을 거쳐 실제 토큰을 발급받아 움직이므로, 장애 시 인증 시스템까지 도미노로 꺾이는지 검증 |
    | ② 서비스 이용 흐름 | GET /api/products ➔ /products/1 | 워커 노드 2가 죽어도, 노드 1에 살아있는 상품 조회 계층은 정상 작동하는 '부분 장애' 상태 유도 |  |
    | ③ 핵심 트랜잭션 차단 | POST /api/orders ➔ POST /api/payments | 워커 노드 2와 함께 다운된 주문/결제 Pod들이 유실되어 유저들의 구매 행위가 전면 차단되는 현상 재현 |  |
- 트래픽 타임라인 및 구간별 상세 목적
    
    
    | **구간** | **목표 RPS** | **구간 명칭** | **인프라 관점의 상세 목적 및 검증 요소** |
    | --- | --- | --- | --- |
    | **0 ~ 5분** | **500 RPS** | **정상 운영 구간** | 실제 유저들이 정상적으로 로그인하고 상품을 조회한 뒤, 주문 및 결제까지 매끄럽게 이어지는 비즈니스 활성화 상태 기록 |
    | **5분 정각** | **500 RPS** | 인프라 카오스
    (장애 발발 시점) | **테스트 진행 중 코어 서비스가 밀집된 '워커 노드 2'를 강제로 종료(Power Off)**
    • "상품 검색은 원활한데 주문/결제하기 버튼만 누르면 터지는" 현실적이고 치명적인 부분 장애 상황을 고의로 유도 |
    | **5 ~ 15분** | **500 RPS** | 장애 유입 및
    자가 치유 관찰 | 장애 복구가 지연되는 와중에도 멈추지 않고 계속 밀려 들어오는 유저 트래픽 속에서 두 환경의 회복 탄력성(Resilience) 계측
    • **온프레미스:** 유실된 Pod들이 남은 워커 노드 1로 재배치되려 하나, 자원 포화로 인해 **Pending(대기)** 상태 지속 ➔ 엔지니어가 개입할 때까지 **수 분 동안 결제 실패 에러가 기하급수적으로 누적**됨을 증명
    • **EKS:** 노드 유실을 감지한 Karpenter가 스케줄러와 즉시 연동하여 **30~60초 이내에 대체 EC2 노드를 자동으로 프로비저닝** ➔ 튕겨 나갔던 Pod들을 즉시 정상 스케줄링하여 스스로 에러를 진압하는 복구 속도 입증 |
    | **15 ~ 20분** | **500 RPS** | **재안정화 검증** | 인프라 자가 치유 완료 후, 전체 비즈니스 파이프라인(로그인~결제)이 단 한 건의 유실도 없이 **에러율 0%로 완벽하게 돌아왔는지 최종 확인** |
- API 부하 비율 및 엔드포인트 상세
    
    
    | **엔드포인트** | **메서드** | **목표 비율** | **비고 및 시나리오 의미** |
    | --- | --- | --- | --- |
    | /api/auth/login | **POST** | **10%** | 유저 유입에 따른 지속적인 세션 생성 및 인증 인프라 체크 |
    | /api/products | **GET** | **40%** | 장애 상황에서도 노드 1의 생존으로 인해 정상 작동하는 상품 조회 트래픽 |
    | /api/orders | **POST** | **30%** | 노드 2 다운으로 인해 Pod가 유실되어 에러가 터지기 시작하는 병목 지점 |
    | /api/payments | **POST** | **20%** | 주문 장애의 여파로 결제 승인이 실패하며 비즈니스 매출 손실로 직결되는 계층 |
- 테스트 최종 목표
    - **온프레미스의 치명타:** 인프라 장애 발생 시 하드웨어가 자동으로 늘어나지 못하므로, 사람이 모니터링을 보고 수동으로 서버를 조치하기 전까지 **수 수백 건의 결제 에러가 고스란히 기업의 매출 손실로 이어지는 리스크** 부각
    - **EKS + Karpenter의 우수성:** 야간이나 주말에 장애가 터지더라도 엔지니어의 개입 없이 **클라우드가 알아서 1분 안에 물리 장비를 교체하고 서비스를 복구(MTTR 최소화)해내는 강력한 무중단 아키텍처** 증명

# ▶︎ Grafana, Prometheus

## ▷ Prometheus + Grafana

- monitoring/prometheus/prometheus.yml
    
    ```jsx
    global:
      scrape_interval: 5s
    
    scrape_configs:
      - job_name: prometheus
        static_configs:
          - targets: ['prometheus:9090']
    ```
    
- monitoring/docker-compose.yml
    
    ```jsx
    services:
      prometheus:
        image: prom/prometheus
        container_name: shoply-prometheus
        ports:
          - "9090:9090"
        command:
          - "--config.file=/etc/prometheus/prometheus.yml"
          - "--web.enable-remote-write-receiver"
        volumes:
          - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    
      grafana:
        image: grafana/grafana
        container_name: shoply-grafana
        ports:
          - "3000:3000"
        depends_on:
          - prometheus
    ```
    

- 실행
    - docker compose up -d
- Grafana 접속
    - [http://localhost:3000](http://localhost:3000/)
- Prometheus Data Source 추가
    - Connections → Data sources → Prometheus
    URL: [http://prometheus:9090](http://prometheus:9090/)
    Save & test

- 모니터링 환경 구성
    - Prometheus Docker 구성 완료
    - Grafana Docker 구성 완료
    - Grafana ↔ Prometheus 연동 완료

# ▶︎ 트러블슈팅

## ▷ frontend nginx Welcome 화면 문제

## 문제

브라우저 접속 시:

```
Welcome to nginx!
```

출력

## 원인

- nginx 기본 index.html 사용
- 실제 프론트 빌드 결과 반영 안 됨

## 해결

- Dockerfile 구조 수정
- TanStack Start 구조 확인
- frontend 실행 방식 변경

## ▷ frontend 컨테이너 즉시 종료 문제

## 문제

```
docker ps
```

시 frontend 컨테이너 미표시

## 원인

- TanStack Start + Cloudflare 기반 구조
- 일반 nginx static 구조 아님

## 해결

- Node 기반 실행 방식으로 Dockerfile 수정
- npm run dev 기반 실행으로 변경

## ▷ frontend 구조 충돌 문제

## 문제

기존:

```
app/shoply/frontend
```

최신 구조:

```
app/shoply/src
```

## 해결

- 최신 팀원 구조 유지
- frontend_old 백업 유지
- 루트 구조 기준으로 통합

---

<aside>
<img src="https://www.notion.so/icons/push-pin_gray.svg" alt="https://www.notion.so/icons/push-pin_gray.svg" width="40px" />

앞으로 할 일 

- CI-CD는 GitHub Actions 를 사용
    - github acitons 사용시  EC2 안만들어도 됨. 직접
    - Jenkins 사용 시 EC2 만들어야함, 비용 발생가능성 높음 (t3.meduim 추천 월 35~50달러 )
    - AI 추천은
    
    | 상황 | 추천 |
    | --- | --- |
    | 시간/비용 절약 | GitHub Actions |
    | DevOps 구축 경험 강조 | Jenkins |
    | 팀 프로젝트 안정성 | GitHub Actions |
    | 포트폴리오 임팩트 | Jenkins 약우세 |
    
    | 항목 | Jenkins | GitHub Actions |
    | --- | --- | --- |
    | 구축 난이도 | 높음 | 낮음 |
    | 운영 편의성 | 낮음 | 높음 |
    | 발표 임팩트 | 강함 | 보통 |
    | AWS 연동 | 좋음 | 매우 좋음 |
    | 온-pre 연결 | 매우 자유로움 | 충분함 |
    | 유지보수 | 어려움 | 쉬움 |
    | 팀 프로젝트 적합성 | 중간 | 높음 |
    | 구현 속도 | 느림 | 빠름 |
- 부하테스트 : k6
- 모니터링 기준 : Prometheus + Grafana
- aws 는 Cloudwatch 를 보조로 활용
    - AWS EKS , 온프레 Kubernetes 사용, Prometheus 방식으로 메트릭 수집 가능
- 비교 지표
    - 공통
        - CPU 사용률
        - Memory 사용률
        - Pod 수
        - 요청 수
        - 응답 시간
        - 에러율
        - 재시작 횟수
        - 노드 상태
</aside>

---

𝘮𝘢𝘥𝘦 𝘣𝘺 𝘠𝘪𝘯𝘢, 𝘕𝘰𝘵𝘪𝘰𝘯 𝘊𝘓 𝘰𝘧 𝘏𝘜𝘍𝘚 … [𝘷𝘪𝘦𝘸 𝘮𝘰𝘳𝘦](https://www.instagram.com/notion_hufs?utm_source=ig_web_button_share_sheet&igsh=ZDNlZDc0MzIxNw==)

/api/products