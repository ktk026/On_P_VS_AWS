apiVersion: apps/v1
kind: Deployment
metadata:
  name: stays-service
  namespace: accommodation
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stays-service
  template:
    metadata:
      labels:
        app: stays-service
    spec:
      containers:
      - name: stays-service
        image: python:3.11.9-slim
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: db-config
        - secretRef:
            name: db-secret

---
apiVersion: v1
kind: Service
metadata:
  name: stays-service
  namespace: accommodation
spec:
  selector:
    app: stays-service
  ports:
  - port: 8000
    targetPort: 8000
