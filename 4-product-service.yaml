apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: accommodation
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
      - name: product-service
        image: product-service:latest
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: db-config
        - secretRef:
            name: db-secret

---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: accommodation
spec:
  selector:
    app: product-service
  ports:
  - port: 8000
    targetPort: 8000
