apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: accommodation
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
      - name: payment-service
        image: payment-service:latest
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: db-config
        - secretRef:
            name: db-secret

---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: accommodation
spec:
  selector:
    app: payment-service
  ports:
  - port: 8000
    targetPort: 8000
