apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: accommodation
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      nodeSelector:
        role: api
      containers:
      - name: api-gateway
        image: ghcr.io/ktk026/shoply-gateway:develop
        ports:
        - containerPort: 4000
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        envFrom:
        - configMapRef:
            name: db-config
        - secretRef:
            name: db-secret

---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: accommodation
spec:
  selector:
    app: api-gateway
  ports:
  - port: 4000
    targetPort: 4000
